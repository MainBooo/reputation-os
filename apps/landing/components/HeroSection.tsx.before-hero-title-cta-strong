import HeroOrbitSvg from './HeroOrbitSvg'

export default function HeroSection() {
  return (
    <section className="relative z-10 mx-auto grid w-full max-w-7xl grid-cols-1 items-center gap-10 px-7 py-20 lg:grid-cols-2 lg:gap-12 xl:px-0">
      <div className="relative z-20 max-w-2xl text-left">
        <h1 className="text-[clamp(64px,6.8vw,112px)] font-semibold uppercase leading-[0.9] tracking-[-0.055em] text-white max-xl:text-[clamp(54px,6vw,88px)] max-sm:text-[clamp(40px,12vw,58px)]">
          ОПЕРАЦИОННАЯ СИСТЕМА<br />
          ВАШЕЙ <span className="text-cyan-300">РЕПУТАЦИИ</span>
        </h1>

        <p className="mt-6 max-w-2xl text-xl leading-8 text-slate-300 max-sm:text-base max-sm:leading-7">
          Единая система для управления отзывами, рейтингами, упоминаниями и репутационными рисками в одном пространстве в реальном времени.
        </p>

        <div className="mt-8 flex">
          <a
            className="inline-flex min-h-[58px] items-center justify-center rounded-2xl border border-cyan-200/30 bg-gradient-to-br from-cyan-300 to-blue-500 px-9 text-base font-extrabold text-slate-950 shadow-[0_0_34px_rgba(34,211,238,0.28)] transition hover:bg-cyan-200"
            href="https://reputation.generationweb.ru/login"
          >
            Войти в систему →
          </a>
        </div>
      </div>

      <HeroOrbitSvg />
    </section>
  )
}
