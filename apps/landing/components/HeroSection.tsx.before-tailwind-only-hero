import HeroOrbitSvg from './HeroOrbitSvg'

const trust = [
  ['14 дней бесплатно', 'Без карты'],
  ['Без обязательств', 'Отмена в 1 клик'],
  ['Безопасность данных', 'Enterprise-уровень'],
  ['Поддержка 24/7', 'Живой эфир']
]

export default function HeroSection() {
  return (
    <section className="relative z-[1] mx-auto grid min-h-[720px] w-[min(calc(100%_-_56px),1180px)] grid-cols-[560px_minmax(0,1fr)] items-center gap-24 overflow-visible pt-20 pb-16 max-lg:min-h-0 max-lg:w-[min(calc(100%_-_32px),1280px)] max-lg:grid-cols-1 max-lg:gap-8 max-lg:pt-12 max-lg:pb-14 max-sm:pt-8">
      <div className="relative z-10 max-w-[620px] text-left max-lg:order-1 max-lg:max-w-full">
        <h1 className="max-w-[620px] text-[clamp(72px,6vw,108px)] font-semibold uppercase leading-[0.9] tracking-[-0.065em] text-white [text-shadow:0_2px_18px_rgba(5,9,20,.96),0_0_38px_rgba(5,9,20,.76)] max-lg:text-[clamp(46px,10vw,70px)] max-sm:text-[clamp(38px,14vw,58px)]">
          ОПЕРАЦИОННАЯ СИСТЕМА<br />
          ВАШЕЙ <span className="text-[#b15cff] [text-shadow:0_0_24px_rgba(168,85,247,.72)]">РЕПУТАЦИИ</span>
        </h1>

        <p className="mt-6 max-w-[560px] text-[17px] leading-[1.6] text-[rgba(226,232,255,.68)] max-sm:text-[15px]">
          Единая система для управления отзывами, рейтингами, упоминаниями и репутационными рисками в одном пространстве в реальном времени.
        </p>

        <div className="mt-7 flex">
          <a
            className="inline-flex min-h-[58px] items-center justify-center rounded-2xl border border-white/10 bg-[linear-gradient(90deg,#2f7cff,#7c5cff_50%,#d946ef)] px-8 text-sm font-extrabold text-white shadow-[0_0_34px_rgba(124,92,255,.52)] transition hover:-translate-y-0.5"
            href="https://reputation.generationweb.ru/login"
          >
            Войти в систему →
          </a>
        </div>
      </div>

      <HeroOrbitSvg />

      <div className="hidden">
        {trust.map(([title, text]) => (
          <div key={title}>
            <i>✦</i>
            <b>{title}</b>
            <span>{text}</span>
          </div>
        ))}
      </div>
    </section>
  )
}
