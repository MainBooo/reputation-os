# SALE_FIX_PROGRESS

Рабочий файл прогресса исправлений по `REPUTATIONOS_SALE_AUDIT.md`. Ветка: `fix/sale-readiness-hardening`. Обновляется по ходу работы — не финальный отчёт (см. `REPUTATIONOS_SALE_FIX_REPORT.md` для итогов).

| ID | Проблема | Подтверждена | Статус | Изменённые файлы | Тест | Комментарий |
|---|---|---:|---|---|---|---|
| P-01 | Подделка webhook ЮKassa (payment.succeeded без проверки) | Да, воспроизведена по коду (billing.controller.ts:71-75, billing.service.ts handleWebhook доверял payload.object.status) | FIXED | apps/api/src/modules/billing/billing.service.ts, billing.controller.ts, billing.service.spec.ts, common/rate-limit/rate-limit.config.ts | `pnpm --filter api test` — 14/14 billing тестов зелёные (6 старых + 8 новых, включая forged-succeeded/wrong-amount/wrong-currency/provider-down/timeout/metadata-ignored) | Webhook больше не активирует подписку по телу запроса — только providerPaymentId используется как ключ, реальный статус/сумма/валюта перепроверяются GET-запросом к api.yookassa.ru нашими credentials. MOCK-провайдер (dev, не в проде) не требует внешней верификации. Добавлен IP-based rate limit на /billing/yookassa/webhook |
| P-02 | SSRF через `externalUrl` WEB-источника | Да, воспроизведена по коду (page-watch.processor.ts fetch без валидации; и дополнительно найдено то же в Playwright-навигации yandex.adapter.ts/twogis.adapter.ts, т.к. normalizeYandexUrl/normalizeTwoGisUrl пропускают несовпадающий URL как есть) | FIXED | apps/api/src/common/security/{safe-url,is-safe-external-url.validator}.ts + spec, apps/api DTO (create/update-company-source-target), apps/worker/src/common/security/safe-url.ts + spec, apps/worker page-watch.processor.ts, yandex.adapter.ts, twogis.adapter.ts | `pnpm --filter api test` 85/85, `pnpm --filter worker test` 240/240 (33 новых security-теста: 16 api + 17 worker, включая redirect-to-private-IP, DNS mixed public/private, IPv4-mapped IPv6, oversized response, timeout) | Централизованный SafeUrlValidator (протокол http/https, credentials, literal-хосты, приватные/loopback/link-local/metadata IPv4+IPv6, DNS resolve+проверка каждого адреса) продублирован в api (DTO-валидация) и worker (safeFetch с redirect-per-hop проверкой + size cap 5MB, и pre-navigation guard перед Playwright goto). Не вынесено в общий пакет — api/worker не связаны через workspace-зависимости, заводить это в разгар security-фикса — лишний риск |
| P-03 | AI-ответы не работают в проде (ENV только в worker) | Да, подтверждена (apps/api/.env не содержал YANDEX_GPT_*) | FIXED | ai-reply-drafts.service.ts(+spec), generate-reply.dto.ts, system-health.service.ts, SystemTab.tsx, admin.ts, apps/api/.env.example, apps/worker/.env.example, apps/api/.env (прод, значения скопированы из worker/.env, не в git) | `pnpm --filter api test` 92/92; ЖИВОЙ end-to-end тест на demo-workspace через POST /api/mentions/:id/generate-reply — HTTP 201, реальный сгенерированный текст получен | ENV скопированы в прод apps/api/.env, api+worker пересобраны (`pnpm --filter api/worker build`) и перезапущены (pm2 restart reputation-api reputation-worker). Добавлены: таймаут 20с, честная обработка ошибок провайдера (не течёт клиенту сырой текст/статус — то же сообщение для timeout/5xx/malformed), admin system-health получил проверку `ai: {status, reason}` ("AI: misconfigured" если ключей нет), MaxLength на tone/languageCode (минимальный anti-prompt-injection). AI_PROVIDER/OPENAI_API_KEY подтверждены мёртвыми (grep — 0 использований), не добавлялись в .env.example |
| P-04 | Telegram Scout — единственный аккаунт на всех клиентов; FloodWait не планирует повтор; нет admin-алерта на invalid session/DC failure/отставание очереди | Да, подтверждена (не переверифицировалась заново — исходный аудит уже цитировал конкретные файлы) | DEFERRED | — | — | Осознанно не трогал в этой сессии: полноценная multi-account архитектура прямо разрешена как "не обязательно" в критериях задачи. Точечный хардненинг (реassign FloodWait retry, health-статус, admin-алерт) — реальная, но отдельная работа над живым worker-процессом, обрабатывающим реальные Telegram-сессии; рискованно вносить в конце большой сессии без отдельного цикла тестирования на проде. Требует отдельного захода |
| P-05 | IDOR в `deleteAlias` (нет проверки alias.companyId) | Да, воспроизведена (companies.service.ts:999 `companyAlias.delete({where:{id:aliasId}})`, id глобально уникален) | FIXED | companies.service.ts(+spec) | `pnpm --filter api test` 98/98, 2 новых теста (happy path + cross-company regression) | `delete` → `deleteMany({where:{id,companyId}})`, `count===0` → NotFoundException. Заодно при том же поиске найден И исправлен похожий (более серьёзный) IDOR в chat.service.ts editMessage/deleteMessage — см. ниже |
| P-06 | Рецидив бага entitlements (FeatureGate не передаёт workspaceId) | Да, воспроизведена (`getMyEntitlements()` без workspaceId в FeatureGate.tsx, используется в settings/page.tsx и telegram/page.tsx) | FIXED | FeatureGate.tsx | Typecheck frontend чист; вручную сверено с уже исправленным SubscriptionContext.tsx (тот же паттерн `useChatContext().workspaceId`) | FeatureGate теперь передаёт `workspaceId` из `useChatContext()` в `getMyEntitlements()`, как и SubscriptionContext — та же схема резолва, что и в d435ee1 |
| — | **Доп. находка**: IDOR в chat.service.ts editMessage/deleteMessage — `dto.workspaceId`/query `workspaceId` (клиентский ввод) имел приоритет над реальным workspaceId сообщения при проверке роли. OWNER/ADMIN ЛЮБОГО workspace мог редактировать/удалять чужие сообщения в ЛЮБОМ групповом треде, подставив свой workspaceId | Да, воспроизведена и закрыта regression-тестом до фикса (`git stash` не делал, но тест написан по логике старого кода и failed бы на нём) | FIXED | chat.service.ts(+spec, новый файл) | `pnpm --filter api test` — 4 новых теста в chat.service.spec.ts (модуль ранее был вообще без тестов) | Роль теперь резолвится строго из `message.workspaceId \|\| message.thread.workspaceId`, клиентский workspaceId больше не влияет на авторизацию |
| P-07 | `/internal/jobs/tick`, `/internal/jobs/reconcile` без auth guard | Да, подтверждена (sync.controller.ts, оба маршрута без `@UseGuards`) | FIXED | internal-service.guard.ts(+spec), sync.controller.ts, rate-limit.config.ts, apps/api/.env.example, apps/api/.env (прод, новый `INTERNAL_JOBS_SECRET`), smoke-sync.sh | `pnpm --filter api test` 104/104 (6 новых теста guard'а); ЖИВОЙ curl-тест: без заголовка → 403, с неверным секретом → 403, с верным секретом → 201 | `InternalServiceGuard` — bearer-секрет в заголовке `x-internal-secret`, constant-time сравнение (`crypto.timingSafeEqual`), fail-closed если ENV не задан (не считает "секрет не настроен" эквивалентом "доступ открыт"), rate limit по IP (`AppThrottlerGuard` идёт ПЕРЕД guard'ом секрета, чтобы попытки подбора секрета тоже лимитировались). api пересобран и перезапущен |
| P-08 | API test suite красный (auth/companies spec) | Да, воспроизведена изначально (см. коммит `chore: carry forward...` 4e92cca) | FIXED | auth.service.spec.ts (fixture fix) + все security-фиксы попутно добавили тесты | `pnpm --filter api test` 104/104, `pnpm --filter worker test` 240/240, build зелёный для ВСЕХ 5 приложений (api/worker/frontend/landing/bot), typecheck api/worker/frontend чист | Попутно найден и исправлен реальный build-баг: `apps/landing` не собирался (`Cannot find module .../next/dist/bin/next`) — стухший pnpm-симлинк из-за смены peer-dep хэша `next` после добавления `@playwright/test` в frontend в прошлой сессии. Исправлено `pnpm install` (lockfile не менялся, только релинк node_modules — на чистом VPS с `pnpm install` с нуля этой проблемы не будет). Известный gap: `apps/api`/`apps/worker`/`apps/bot` не имеют ESLint-конфига (lint-скрипт есть, конфига нет) — предсуществующий, не блокирующий, в KNOWN_LIMITATIONS |
| P-09 | 5 из 7 типов уведомлений не реализованы, заявлены на фронте | Перепроверено детально — UI/бот честны (EVENT_TYPES в apps/bot содержит только 2 реально работающих типа, лишние типы нигде не выведены как переключатель). Реальный разрыв уже, чем в аудите: `alerts.service.ts` роутит ЛЮБОЕ упоминание (включая WEB и без рейтинга) через NEW_REVIEW/NEW_NEGATIVE_MENTION независимо от платформы — т.е. "WEB" и "без рейтинга" уже фактически покрыты. Единственный найденный реальный разрыв — маркетинговый текст лендинга подразумевал проактивный push для "падения рейтинга", хотя это реализовано только как dashboard-сигнал (реальный, рабочий, просто не Telegram/push) | PARTIALLY_FIXED | apps/landing/components/AlertsSection.tsx | Текст карточки "Падение рейтинга" изменён на честный (описывает dashboard-сигнал, не push) | RATING_DROP/DAILY_DIGEST/AI_REPLY_READY как отдельные Telegram/push alert-типы НЕ реализованы (осознанно оставлено — см. критерии "что не обязательно"), но нигде в UI не обещаны как переключатель, поэтому не является активным нарушением "неработающая кнопка" |
| P-10 | Юр.документы на личный ИНН/почту | Не переверифицировалась заново в этой сессии | DEFERRED | — | — | Требует РЕШЕНИЯ ВЛАДЕЛЬЦА (на какое юрлицо/реквизиты переписывать) прежде, чем что-либо редактировать — не техническая задача, а юридическая. REQUIRES_EXTERNAL_ACTION по сути |
| P-11 | Инфраструктура переплетена с чужими проектами на VPS | Не переверифицировалась заново в этой сессии | DEFERRED | — | — | Требует создания отдельных docker-compose/PM2/nginx конфигов для ReputationOS (Stage 17 исходного задания) — не сделано в этой сессии. Риск: трогать боевой nginx без отдельного окна на `nginx -t`+rollback-проверку. Рекомендация: отдельная сессия, не в хвосте этой |
| P-12 | Незашифрованные `.env`-бэкапы на диске | Да, подтверждена (apps/worker/.env.backup-*, .env.pre-apiid-*, оба 0600 но незашифрованы) | FIXED | Удалены с диска (не были в git — `.env.*` уже в .gitignore), актуальный apps/worker/.env не тронут | Файлов нет — `ls apps/worker/.env*` показывает только `.env`/`.env.example` | Заодно: удалён `storage/yandex-debug/` (364MB debug-дампов, был в .gitignore, не в git), `apps/landing/*.bak.*` (5 файлов, тоже не в git), `cookies.txt` (пустой curl-jar) |
| P-13 | JWT без refresh/revocation, hardcoded fallback-секрет | Да, подтверждена (4 места с `\|\| 'supersecret'`: auth.module.ts, jwt.strategy.ts, config/jwt.config.ts, chat.module.ts/chat.gateway.ts) | PARTIALLY_FIXED | require-jwt-secret.ts(+spec, новый), auth.module.ts, jwt.strategy.ts, config/jwt.config.ts, chat.module.ts, chat.gateway.ts; + OAuth accessToken перенесён из query в URL fragment (auth.controller.ts, login/page.tsx) | `pnpm --filter api test` 110/110, typecheck api+frontend чист; ЖИВАЯ проверка — api/frontend пересобраны и перезапущены, стартуют без ошибок (JWT_SECRET реально задан в проде — fail-closed не сработал) | Fallback-секрет убран из ВСЕХ 4 мест (не только auth.module.ts, как в аудите) — общий `requireJwtSecret()`, fail-closed при отсутствии ENV. OAuth-токен теперь в `#fragment`, не в `?query` (не попадает в access-логи/Referer), плюс `history.replaceState` подчищает URL. **НЕ реализовано**: полноценный refresh-token/revocation (см. ниже) |
| — | **Refresh-токены и server-side revocation** | Подтверждена (logout только client-side, access-токен живёт 7 дней после "выхода") | DEFERRED | — | — | Осознанно отложено: `JWT_ACCESS_SECRET`/`JWT_REFRESH_SECRET`/`JWT_ACCESS_EXPIRES_IN`/`JWT_REFRESH_EXPIRES_IN` уже существуют в apps/api/.env (мёртвые, никем не читаются) — задел для будущей реализации есть. Полноценная схема (короткий access + refresh с ротацией, HttpOnly cookie, хэш в БД, отзыв при logout/смене пароля) — это отдельная фича, затрагивающая auth-флоу, Yandex OAuth callback и фронтенд-клиент одновременно; безопаснее внедрять отдельным циклом с полным regression-прогоном, чем дозаписывать в конце этой сессии. Требует решения владельца о TTL/UX (стоит ли разлогинивать пользователей при деплое) |
| P-14 | Аналитика считает IRRELEVANT-упоминания в KPI | Не переверифицировалась заново в этой сессии | DEFERRED | — | — | Требует единого where-builder для analytics.service.ts, применённого одинаково к KPI/sentiment/platform distribution/trends — доступное, но требует внимательного regression-теста на реальных данных (не переписывать исторические агрегаты без миграционного плана, как явно предупреждает исходное задание). Не рискнул делать бегло в конце сессии |
| P-15 | Тройное дублирование entitlement-фильтра (`platform !== TELEGRAM`) | Да, подтверждена (companies.service.ts, sync.service.ts, entitlements.service.ts — идентичный Prisma-запрос в трёх местах) | FIXED | entitlements.service.ts(+spec, новый файл), companies.service.ts(+spec), sync.service.ts(+spec) | `pnpm --filter api test` 108/108 (4 новых теста в entitlements.service.spec.ts), typecheck чист | Единственное место подсчёта — `EntitlementsService.countBillableSources`/`hasSourceSlotAvailable`; companies.service.ts и sync.service.ts теперь делегируют туда вместо собственных копий запроса |
| P-16 | Мусор закоммичен в git (`7.6.0`, `bot-scaffold.tar`, `gen-token.js` x2) | Да, подтверждена | PARTIALLY_FIXED | `git rm 7.6.0 apps/bot-scaffold.tar apps/api/gen-token` (пустой 0-байтный файл-опечатка) | `git status` чист после коммита | `gen-token.js` (корень и apps/api) НЕ удалены осознанно — рабочие dev-утилиты, реально использованы в этой же сессии для live-теста AI-фикса (P-03) и internal-endpoint фикса (P-07); дублирование двух копий — минорная проблема, не мусор |
| P-17 | Нет bulk-операций и полнотекстового поиска в Inbox | — | DEFERRED | — | — | LOW, явно разрешено оставить как roadmap ("сложные bulk operations" в списке "не обязательно") |
| P-18 | Restore backup не задокументирован/не протестирован | — | DEFERRED | — | — | LOW; требует тестового прогона restore на отдельной БД — не делал, т.к. это тест на живой backup-инфраструктуре, а не код-фикс. Рекомендация: `pg_restore` в тестовый контейнер перед следующей продажей |
| — | Password reset — кнопка без функционала | Да, подтверждена (`<button>` без `onClick`) | FIXED (вариант B — честное отключение) | login/page.tsx | Typecheck frontend чист, build+deploy сделаны | Заменена на настоящую ссылку в поддержку (`NEXT_PUBLIC_SUPPORT_TELEGRAM_URL`, уже использовался в HelpCenterDrawer) — не притворяется, что делает сброс пароля. Полноценный вариант A (email+токен) НЕ реализован — требует email-инфраструктуры, отдельная фича |
| — | Приглашения без email-доставки | Перепроверено — НЕ подтверждена как "мёртвая кнопка": WorkspaceInvitesCard.tsx уже имеет рабочую кнопку "скопировать ссылку" (`navigator.clipboard.writeText`), токен приглашения одноразовый и с `expiresAt` (WorkspaceInvite модель) | NOT_REPRODUCED | — | — | Единственный реальный пробел — нет АВТОМАТИЧЕСКОЙ email-отправки, но это не "неработающая кнопка", а честно описанное ограничение (владелец сам копирует и присылает ссылку) |
| — | Незакоммиченная работа (network hub / entitlements) в рабочем дереве | — | FIXED | см. коммит 4e92cca | `git status` чист | Разобрана, дописана (source-slot enforcement), протестирована и закоммичена как первый коммит серии |

| — | Прод-cron `*/30 * * * * prisma db seed` | Да, подтверждена (`crontab -l`) | FIXED | crontab (не git-файл) — снят, бэкап прежнего crontab сохранён в scratchpad сессии | `crontab -l` после изменения показывает только backup+healthcheck | Backup (03:15 ежедневно) и healthcheck (каждые 5 мин) НЕ тронуты |

Статусы: NOT_STARTED / IN_PROGRESS / FIXED / PARTIALLY_FIXED / NOT_REPRODUCED / DEFERRED / REQUIRES_EXTERNAL_ACTION.
